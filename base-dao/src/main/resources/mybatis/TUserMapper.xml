<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gsean.dao.TUserMapper">
  <resultMap id="BaseResultMap" type="TUser">
    <!--
      WARNING - @mbg.generated
    -->
    <id column="id" jdbcType="BIGINT" property="id" />
    <result column="name" jdbcType="VARCHAR" property="name" />
    <result column="email" jdbcType="VARCHAR" property="email" />
  </resultMap>





  <select id="selectUserPermission" resultType="UserPermissionResp" parameterType="map">
    SELECT
      t_user.id,t_user.`name`,role.id role_id,role.`name` role_name,
      permission.id permission_id,permission.`name` permission_name,permission.en_name,url
    FROM t_user
    LEFT JOIN user_role ON t_user.id =user_role.user_id
    LEFT JOIN role ON user_role.role_id=role.id
    LEFT JOIN role_permission ON role.id=role_permission.role_id
    LEFT JOIN permission ON role_permission.permission_id=permission.id
    <trim prefix="where" prefixOverrides="and |or">
      <if test="@org.apache.commons.lang3.ObjectUtils@isNotEmpty(userId)">and t_user.`id`=#{userId}</if>
      <if test="@org.apache.commons.lang3.ObjectUtils@isNotEmpty(userName)">and t_user.`name`=#{userName}</if>
      <if test="@org.apache.commons.lang3.ObjectUtils@isNotEmpty(isDeleted)">and t_user.`is_deleted`=#{isDeleted}</if>
    </trim>
  </select>
</mapper>